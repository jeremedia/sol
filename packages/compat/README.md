# `@jeremedia/sol-compat`

This package is reserved for the migration bridge.

Planned contents:

- legacy atomic class compatibility
- legacy JavaScript entry points that proxy to the modern runtime
- deprecation guidance and migration fixtures
