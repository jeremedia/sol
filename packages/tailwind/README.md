# `@jeremedia/sol-tailwind`

This package will expose Tailwind-facing adapters for the modern Sol token layer.

Planned contents:

- token-aware preset values
- optional plugin helpers for typography, spacing, and color access
- no core design values of its own
